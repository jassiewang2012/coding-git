1. git init - create a new Git repository
2. git status - inspects the contents of the working directory and staging area
3. git add - adds files from the working directory to the staging area 
4. git diff – shows the difference between the working directory and staging area
5. git commit - permanently stores file changes from the staging area in the repository
6. git log shows a list of all previous commits

7. git checkout HEAD filename - Discards changes in the working directory
8. git reset HEAD filename - Unstages file changes in the staging area
9. git reset commit_SHA - Resets to a previous commit in your commit history (7 digital)
10. get add filename_1 filename_2 - Add multiple files to the staging area with a single command

11. git branch - lists all a Git project's branches
12. git branch branch_name - creates a new branch
13. git checkout branch_name - Used to join file changes from one branch to another
14. git merge branch_name - Used to join file changes from one branch to another
15. git branch -d branch_name - Deletes the branch specified

16. git clone - Create a local copy of a remote
17. git remote -v - Lists a Git project's remotes
18. git fetch - Fetches work from the remote into the local copy
19. git margin origin/master - Merges origin/master into your local branch
20. git push origin <branch_name> - Push a local branch to the origin remote
21. git remote set-url origin git@github.com:User/UserRepo.git - used to change the url of an existing remote repository

